{
    "name": "koin-netplay-signaling",
    "private": true,
    "version": "0.1.0",
    "description": "Reference signaling server for koin.js/netplay — WebSocket relay + Cloudflare Realtime TURN credential minting. Optional: koin.js/netplay accepts any signalingUrl or a custom onSignal transport.",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250101.0",
        "typescript": "^5.0.0",
        "wrangler": "^3.90.0"
    }
}
